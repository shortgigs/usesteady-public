/**
 * Replay worker eligibility defaults (pure gate — no dispatch).
 * @see docs/product/replay-worker-eligibility-implementation-contract-v1.md
 */

/** Default replay worker eligibility TTL — 5 minutes (INV-RWORK-ELIG-IMPL-6). */
export const REPLAY_WORKER_ELIGIBILITY_TTL_MS = 300_000;
