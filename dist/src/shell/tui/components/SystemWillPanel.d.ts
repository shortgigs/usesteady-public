/**
 * TUI v1 — SystemWillPanel component.
 *
 * Renders only when systemWill is present.
 * Hidden entirely when absent — no placeholder, no empty box.
 */
type Props = {
    readonly description: string | undefined;
};
export declare function SystemWillPanel({ description }: Props): import("react/jsx-runtime").JSX.Element | null;
export {};
//# sourceMappingURL=SystemWillPanel.d.ts.map