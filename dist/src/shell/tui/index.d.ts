/**
 * TUI v1 — Public entry point.
 *
 * Called by main.ts when USESTEADY_TUI=true.
 * Dynamically imports app.tsx so ink is only loaded when the TUI is active.
 */
export declare function startTui(): void;
//# sourceMappingURL=index.d.ts.map